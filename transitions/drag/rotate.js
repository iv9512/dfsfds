import a from "/index.js"

const rotate = () => {
    const mouse = elMouseIdentifier(el)

    
}

const initiateEvent = (eventType) => {
    fdsfs.use();
}

addEventListener()
Array.from(arr, r => rotate(r));
[...document.getElementsByClassName("rotate")].forEach(r => rotate(r));